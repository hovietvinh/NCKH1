import ContentElement from "../../ContentElement";


function Chat(props) {
    // const message =[];
    const {id} = props
    // console.log(reLoadAll);
    return (
        <>
            <ContentElement   id={id}/>
        
        </>
     
    );
}

export default Chat;